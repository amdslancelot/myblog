set ip "172.17.0.2"
set port "8000"

yum install -y wget
yum install -y vim
yum install -y expect
wget https://bootstrap.pypa.io/get-pip.py
python get-pip.py
pip install mezzanine
mezzanine-project myblog
export LC_ALL=en_US.UTF-8
cd myblog

/usr/bin/expect << EOF

spawn python manage.py createdb
set timeout 20
expect "Please enter the domain and optional port in the format 'domain:port'." {send "\r"}
expect "Username" {send "\r"}
expect "Email address" {send "lansoulot@gmail.com\r"}
expect "assword" {send "lans0601\r"}
expect "assword (again)" {send "lans0601\r"}
expect "install some initial demo pages" {send "yes\r"}
